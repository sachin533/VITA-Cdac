Q1.

import java.util.Scanner;

public class Que1{
    public static void main(String args[]){
        Scanner sc = new Scanner(System.in);
        String statment = "I have 2 Car one is Baleno Car and other is Farari Car but Truck is used for transportation";

        System.out.println("Enter a word to search:");
        statment = statment.toLowerCase();
        String word = sc.nextLine();
        word = word.toLowerCase();

         int count = 0;
        int index = 0;

                while ((index = statment.indexOf(word, index)) != -1) {
                    count++;
                    index = index + word.length();
                }

                System.out.print(word + " occur : " + count);
                sc.close();
        
    }
}





Q2.

import java.util.Scanner;

public class Que1{
    public static void main(String args[]){
        Scanner sc = new Scanner(System.in);
          System.out.println("Enter a statment to search:");
        String statment = sc.nextLine();

        System.out.println("Enter a word to search:");
        String word = sc.nextLine();
        statment = statment.toLowerCase();
        word = word.toLowerCase();

         int count = 0;
        int index = 0;

                while ((index = statment.indexOf(word, index)) != -1) {
                    count++;
                    index = index + word.length();
                }

                System.out.print(word + " occur : " + count);
                sc.close();
        
    }
}


Q3.

import java.util.Scanner;

public class Que1 {

    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter a word: ");
        String word = sc.nextLine().toLowerCase();

        String original = word;
        String reverse = "";

        for (int i = word.length() - 1; i >= 0; i--) {
            reverse += word.charAt(i);
        }

        if (original.equals(reverse)) {
            System.out.println("Palindrome");
        } else {
            System.out.println("Not a Palindrome");
        }

        sc.close();
    }
}




Q4.


import java.util.Scanner;

public class Que1 {

    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter a word: ");
        String word = sc.nextLine().toLowerCase().trim();

        String original = word;
        int start  = 0;
        int end = word.length()-1;

        int count = 1;
        while(start <end){
            if(word.charAt(start) == ' '){
                start++;
            }else{
                count++;
                start++;
            }
        }
        System.out.println(count);

        sc.close();
    }
}